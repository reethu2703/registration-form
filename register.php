<?php
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    // Process form data (e.g., validate, save to database)
    $email = $_POST["email"];
    $password = $_POST["psw"];

    // Add your logic here (e.g., database insertion)
    // Redirect to a success page or display an error message
}
?>
